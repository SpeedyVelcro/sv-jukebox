- Adjusted time label rounding so that if you place a `TimestampType.POSITION`
  and `DIFFERENCE` or `DIFFERENCE_NEGATIVE` time label in the same
  scene, they should tick through each second at the same time.

